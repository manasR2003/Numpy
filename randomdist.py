import numpy as np

x = np.random.choice([3, 5, 7, 9], p=[0.1, 0.3, 0.6, 0.0], size=(100))

print(x)

#here p=[0.1, 0.3, 0.6, 0.0] means the total sum of all values should be 1 if it o.o means that value will  not appear at any time if it is 1 means it is the maximum appear.
